Algoritmo "questao8"

Var

   a, b, w, x, y, z : real

Inicio

   escreval("Digite 2 numeros: ")
   leia(a, b)
   
   w <- a + b
   x <- a - b
   y <- a * b
   z <- a / b
   
   escreval("os numeros sao: ", w, " ", x, " " , y, " ", z)

Fimalgoritmo